using UnityEngine;
using System.Collections;

public class Serveillance : MonoBehaviour
{
	void Start()
	{
		transform.localScale = new Vector3(-0.2, 0.2, 0.2);
	}

	void Update()
	{
		transform.LookAt(Camera.main.transform);
	}
}